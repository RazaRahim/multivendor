<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.megastock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:replace="android:theme">
        <activity android:name=".ui.buyer.activity_product_detail"></activity>
        <activity android:name=".ui.buyer.showsellers" />
        <activity android:name=".ui.buyer.showproducts" />
        <activity android:name=".prac" />
        <activity android:name=".ui.Seller.updateproduct" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.Seller.sellersignup" />
        <activity android:name=".ui.buyer.buyersignup" />
        <activity android:name=".ui.Seller.SellerEdit" />
        <activity android:name=".ui.buyer.BuyerEdit" />
        <activity android:name=".ui.Seller.selleractivity" />
        <activity android:name=".ui.Seller.AddProducts" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.CALL_PHONE" />

</manifest>